/** Component's element(s) selector(s) */
export const ROOT = ":scope > div > label";
export const INPUT = `${ROOT} > input`;
export const SWITCH_WRAPPER = `${ROOT} > .proi-switch-wrapper`;
export const SWITCH = `${SWITCH_WRAPPER} > .proi-switch`;
export const SWITCH_BULLET = `${SWITCH} > .proi-switch-bullet`;
export const SWITCH_LABEL = `${SWITCH_WRAPPER} > .proi-switch-label`;
