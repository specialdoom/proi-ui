import { default as Modal } from './Modal.svelte';
import { default as ModalBody } from './ModalBody.svelte';
import { default as ModalActions } from './ModalActions.svelte';

export { Modal, ModalBody, ModalActions };
